# export everything (in development mode)
# exportPattern(".*")

export(dependencies, hazPackages)
export(callRaw, mListAllIds, mGet)
export(readIds)
export(demo2, scrubIds, scrapeResources)

exportMethods (dist)
export (normalize, remove.singletons, randomize)

# CLASS AND METHOD EXPORTS
# our S3 classes: metadata
# our S4 classes: sigtest, selection, collection
exportClassPattern(".*")

exportMethods (print, summary, show)
exportMethods ("[[", "$", "[[<-", "$<-")
exportMethods (samples, names, "names<-", groups, "groups<-", rownames, metadata, views, viewnames, "viewnames<-", selection, 
	collection)
exportMethods (pco, heatmap, sigtest, render, asFile)

S3method (print, metadata)
S3method (summary, metadata)
S3method (`[`, metadata)
S3method (print, selection)
S3method (summary, selection)
S3method (print, collection)
S3method (summary, collection)
# S3method (print, pca)
# S3method (summary, pca)
# S3method (print, heatmap)
# S3method (summary, heatmap)

# IMPORTS FROM STANDARD PACKAGES
importClassesFrom (Matrix, Matrix)

