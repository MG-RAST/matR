
#---------------------------------------------------------------------
#  biom -> biom (transformations)
#---------------------------------------------------------------------

normalize.biom		<- function (x, method, ...) {
	}

remove.singletons.biom <- function (x, ...) {
	}

randomize.biom		<- function (x, ...) {
	}

#---------------------------------------------------------------------
#  biom or matrix -> product (utilities)
#---------------------------------------------------------------------

normalize.matrix 	<- function (x, method) {
	}

randomize.matrix 	<- function (x, ...) {
	}

remove.singletons.matrix	 <- function (x, ...) {
	}

stats.biom			<- function(x, ..., rows=, cols=) {
	}

stats.matrix		<- function(x, ...) {
	}

dist.biom			<- function (x, ...) {
	}

dist.matrix			<- function (x, ...) {
	}
